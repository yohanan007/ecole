<form method="post" action="{{ path('app_parent_eleve_delete', {'id': parent_eleve.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parent_eleve.id) }}">
    <button class="btn">Delete</button>
</form>
